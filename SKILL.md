---
name: icpml-skill
description: Use when iC asks to create, revise, package, audit, or troubleshoot a PyMOL PML, protein structural overlay, MD-derived structure view, interaction-site display, or PyMOL object-panel layout.
---

# iC PyMOL

## 目标与范围

交付可直接加载、科学含义明确、右侧对象栏干净的 PyMOL 文件。主要服务蛋白体系；遇到核酸、膜、材料或其他非典型体系时，先询问用户希望如何表示背景和关注区域，不擅自套用蛋白规则。

这不是固定模板。不得在通用流程中假定链名、残基编号、序列长度、配体名称、原子编号或蛋白必然带 peptide。项目固定值只能放在明确匹配的可选预设中。

## 工作流程

1. 先确认用户想比较什么、哪些对象是背景、关注对象、反应或相互作用区域、比较状态，以及是否需要测量。
2. 检查实际输入结构的对象、链、残基、原子、altloc 和氢原子情况；从文件和上下文推导选择，不凭文件名猜测。
3. 若上下文足以判断反应区域，直接作出克制的选择；若歧义会改变科学含义或画面重点，只问一个简短问题再继续。
4. 建立语义真实且尽量互斥的最终对象，完成对齐、表示、颜色、镜头和对象栏整理。
5. 生成版本化 PML，并按实际可用条件完成静态或真实渲染验收。

## 角色与选择

- **背景/支架**：提供空间参照的蛋白主体。通常使用低饱和、半透明 cartoon，必要时只增加局部 surface。
- **关注对象**：短 peptide、小分子、辅因子或底物默认可完整显示 `not elem H` sticks；大分子不默认整条展开 sticks。
- **反应/相互作用区域**：只展开与当前问题相关的侧链或局部原子。依据用户描述、已知功能位点、直接接触、氢键/盐桥或结构比较酌情判断，不固定一个全局距离阈值。
- **比较状态**：使用可区分且不暗示未证实结论的稳定配色。已有验收配色时优先沿用。
- **独立区域**：用户要求某个 groove、domain 或 site 单列时，其对象只含该区域；同层其他展示对象默认排除这些原子，除非用户明确需要重叠。

从轨迹提取 Protein 或复合物时，必须使用匹配的拓扑先处理 PBC，不能直接把 raw XTC/TRR 抽帧交给 PyMOL。通常先使分子完整（whole/molecule repair）；多链复合物可能分散到不同盒像时，再以合适的完整复合物分组执行 cluster/center，最后才在科学上合适的共同原子集上对齐。PBC 整理与旋转拟合应保持为可核查的独立步骤，不把它们混成一个无法判断问题来源的命令。链字段缺失时，改用当前体系已经核验的 atom ID、residue mapping 或拓扑选择，不硬套 `chain`。

## 对象栏合同

- 对象名必须准确描述其实际原子内容；不能加载完整复合物后仅靠 `hide` 冒充 peptide-only、ligand-only 或 site-only 对象。
- 小型比较默认保持有序的扁平对象栏，不创建无意义的 `STRUCTURES`、`ALL` 等总组。
- 只有在状态很多、层级确实降低混乱，或需要把测量绑定到结构时才建立语义 group。
- 可使用临时 full-structure 对象完成对齐，再通过 `create` 生成最终子集，并 `delete` 运行时临时对象。这里的 `delete` 绝不删除磁盘源文件。
- 最终不残留无用途的临时对象、named selections、空 group 或测量对象。

## 距离与标签

- 默认不添加 residue label、距离虚线或数字；仅在用户要求或它们对当前解释确有作用时添加少量关键项。
- 每个距离 measurement 必须作为对应结构的附属对象，和该结构放入一个简洁的基础 group。用户通过该 group 一次显示或隐藏结构、虚线和数字，不应再单独清理残留 label。
- measurement object 名称要表明所属结构与测量含义，不使用 `dist01` 一类无语义名称。
- 白背景下数字使用深色高对比文本。不要使用白色描边；为得到视觉上的无描边效果，将 `label_outline_color` 设为与 `label_color` 相同，并关闭 label shadow。距离线颜色与所属结构协调但要清晰可见。
- 若只需数值而不需图上虚线，使用 `get_distance` 或离线计算，不创建多余 measurement object。

## 默认视觉语言

- 默认白背景、一个低饱和半透明蛋白背景；不要让多个重复的全蛋白遮挡关注区域。
- sticks 默认排除氢原子，关键原子或侧链可以适度加粗；不要把“附近所有残基”机械地全部展开。
- 水、离子和无关配体默认隐藏，但与反应问题直接相关者应保留。
- 镜头围绕可见背景与全部关注对象；不把整盒、隐藏对象或已删除的临时对象计入 zoom。
- 标签和测量默认克制，画面可读性优先于信息堆积。

## 交付与验收

- 保护原件；修订写入版本化 sibling，不覆盖已验收文件。
- 默认交付版本化 `.pml`。输入位于同一稳定目录时使用相对路径；只有来源分散或路径不可移植时才建立便携目录，并只复制实际用到的 PDB/GRO 等轻量结构。不要复制大型轨迹。
- **Source QC**：核对结构来源、帧时间、匹配拓扑、PBC/对齐谱系和原件未改变。轨迹抽帧还要确认实际选择的 center/cluster/output group、Protein 原子数，以及至少一次坐标连续性或目视检查，排除共价连接区域被周期盒切开的情况。
- **Static QC**：检查 load 目标、相对路径、选择引用、非空对象、对象真实原子组成、必要的互斥关系、group 成员和最终对象名单。
- **Render QC**：只有在真实 PyMOL 中无加载/命令错误并目视检查后才可 PASS；没有 PyMOL 时明确写 `STATIC PASS / RENDER NOT_RUN`。
- 只做能捕获错误结果的必要检查，不为简单视图扩展成大型验证框架。
- 视觉可用于排除明显错误或提出候选，但不能单独证明自由能、动力学、收敛、盆地归属或因果机制。

## 项目预设路由

仅当工作区或用户上下文明示 TCR_DA、pMHC、standard C/D 或 pTrp6 时，读取 [TCR_DA pMHC 预设](references/tcr-da-pmhc.md)。预设中的固定编号和颜色不得泄漏到其他项目。

## 禁止模式

| 禁止 | 应改为 |
|---|---|
| 固定假设“蛋白 + peptide” | 从实际组成和用户目标分配角色 |
| 在通用流程写死 chain/resi/id | 当前结构核验后再生成选择 |
| `peptide` 对象仍含完整 Protein | 对齐后创建真实 peptide-only 对象 |
| 大范围 sticks 淹没反应区域 | 短关注对象完整显示，大分子只展开相关区域 |
| 从 raw 轨迹直接提 Protein 后加载 | 匹配拓扑下先 whole，必要时 cluster/center，核验无跨盒后再拟合和导出 |
| 少量对象仍套一个大总组 | 默认扁平；只为真实层级或测量绑定分组 |
| 隐藏结构后距离数字仍悬空 | 结构与 measurement 放进同一基础 group |
| 白背景配白色 label 描边 | 深色 label，outline 同色，shadow 关闭 |
| 静态脚本通过就称视觉验收 | 明确区分 STATIC 与 RENDER |
